[server]
port = 3737

[projects]
dev_dirs = ["~"]
skip = [".git", "node_modules", "__pycache__", ".venv", "venv"]

[github]
pat = ""

[auth]
# Optional: set a token to require "Authorization: Bearer <token>" on all /api/* requests.
# Leave empty to disable auth (default for local use).
token = ""

[health]
commit_weight = 40
dirty_weight = 20
readme_weight = 20
description_weight = 20
